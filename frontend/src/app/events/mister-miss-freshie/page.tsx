import Link from "next/link";
import { VOTING_CATEGORIES } from "@/config/genesis";
import { Countdown, PageShell, SectionHeading, VoterPreview } from "@/components/genesis-ui";
import { getISTInstant } from "@/lib/genesisTime";

export default function MisterMissPage() { return <PageShell><main className="page-main"><section className="spotlight-heading"><span className="eyebrow">GENESIS SPOTLIGHT · 04</span><h1>Mister &amp; Miss<br /><em>Freshie.</em></h1><p>A celebration of confidence, character, and campus charm.</p><Countdown target={getISTInstant("2026-09-13", "20:30")} /><Link className="preview-jump" href="#voter-preview">Preview voter view <span>↓</span></Link></section><section className="section"><SectionHeading kicker="THE SHORTLIST" title="Meet the candidates." /><div className="candidate-grid">{["Aarav S.", "Ananya R.", "Kabir M.", "Meera P."].map((name, index) => <article className="candidate" key={name}><div className="candidate-number">0{index + 1}</div><h3>{name}</h3><p>Candidate {index + 1}</p></article>)}</div></section><section className="section voting"><SectionHeading kicker="VOTING CATEGORIES" title="What makes a freshie unforgettable?" /><div className="category-grid">{VOTING_CATEGORIES.map(category => <article className="category" key={category.id}><span>↗</span><h3>{category.name}</h3><p>{category.description}</p></article>)}</div><div id="voter-preview"><VoterPreview /></div></section><section className="results section"><span className="eyebrow">THE RESULT</span><h2>Results arrive<br /><em>after the spotlight.</em></h2><p>Mock results will appear here during the next phase.</p></section></main></PageShell> }